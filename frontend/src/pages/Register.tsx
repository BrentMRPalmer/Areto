import RegisterForm from "../components/RegisterForm"; // Import the Register component

const Register = () => {
  return (
    <div className="px-32 mt-12">
      <RegisterForm />
    </div>
  );
};

export default Register;
